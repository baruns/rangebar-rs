[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "rangebar-rs"
version = "0.1.4"
requires-python = ">=3.9"
dependencies = ["polars>=1.0"]

[tool.maturin]
features = ["pyo3/extension-module"]
